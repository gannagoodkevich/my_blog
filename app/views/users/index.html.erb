<% content_for :title do %>
  <%= Organization.find(params[:organization_id]).title %>
<% end %>

<div class="row">

  <div class="col-sm-3"></div>
  <div class="col-sm-6">
    <h1><%= Organization.find(params[:organization_id]).title %>'s users</h1>
    <div class="col-sm-12" style="background-color:#63c0de"></div>
  </div>
  <div class="col-sm-3"></div>
</div>
<div class="row"><p></p></div>
  <div class="row">
  <div class="col-sm-3"></div>
  <div class="col-sm-6">
    <table class="table table-striped">
      <tr>
        <th>Name</th>
        <th>Status</th>
      </tr>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.name, organization_user_path(Organization.find(params[:organization_id]), user) %></td>
          <td><% if user.active %>
              <%= "active" %>
              <%else %>
            <%="inactive" %>
              <%end%>
          </td>
      <% end %>
    </table>
    <%= paginate @users, window: 2%>

  </div>
  <div class="col-sm-3">

  </div>

</div>
<div class="row">
  <div class="col-sm-5"></div>

  <div class="col-sm-7">
    <div class="col-sm-1"></div>
    <div class="col-sm-11"><%= button_to "https://google.com", method: "get", class: "btn btn-info" do%>
        Create user
      <%end %></div>
  </div>
  </div>
</div>
